# 开课准备 · 授课环境对齐

> 每节课开始前花 1 分钟对一下环境,后面就不会「连蒙带猜」了。

---

## 一、装环境(只需一次)

在项目根目录打开终端:

```bash
pip install -r requirements.txt
```

装好后验证:

```bash
python -c "import numpy, matplotlib; print(numpy.__version__, matplotlib.__version__)"
```

能打印出两个版本号就算成功。

---

## 二、每节课要打开的三样东西

| # | 打开什么 | 干什么用 |
|---|---|---|
| 1 | **教材 PDF** `LA_XX_YY_标题.pdf` | 跟着看原文、看插图 |
| 2 | **配套 notebook** `LA_XX_YY_NN.ipynb` | 教材自带的示范代码(**参考答案,别改**) |
| 3 | **本节代码** `练习/LA_XX_YY_代码.py` | 我整理的完整可运行版,**照着跑** |

助教在每节开头会把这三个的**具体文件名列出来**,不用自己找。

---

## 三、代码怎么跑(三选一)

### 方式 A · 直接跑整个脚本(最省事,推荐)

```bash
python 练习/LA_01_02_代码.py
```

一次跑完全节,输出带小标题,对照讲义看。

### 方式 B · 交互式 REPL(想边试边改)

```bash
python
```

进入 `>>>` 提示符后**逐行粘贴**。

> ⚠️ **REPL 的两个坑**
> 1. **变量不会自动存在**。要用 `e1`,必须先自己敲过 `e1 = np.array([1, 0])`。
>    关掉窗口重开,之前定义的全没了,要重新来一遍。
> 2. **每段代码都要先 `import numpy as np`**。这是所有代码的第一行。
>
> 为避免这两个坑,`练习/LA_XX_YY_代码.py` 里**每一节都是自包含的**——
> 从 `import` 到变量定义都齐全,整段复制粘贴就能跑。

### 方式 C · Jupyter Notebook(想打开教材自带的 .ipynb)

教材的 `LA_*.ipynb` 需要 Jupyter 才能打开:

```bash
pip install notebook
jupyter notebook
```

浏览器会自动弹出,点开对应的 `.ipynb` 文件,按 `Shift + Enter` 逐格运行。

---

## 四、Python 新手最容易踩的括号坑

```
np.linalg.norm( [3, 4] )
              ↑ ↑    ↑ ↑
              │ └列表┘ │
              └─调用───┘
```

**函数永远用 `( )` 调用;`[ ]` 只在两个场合出现——造列表、取下标。**

对照一下常见错误:

| 写法 | 结果 |
|---|---|
| `np.linalg.norm(3, 4)` | ❌ `ValueError` —— 圆括号里的逗号 = 两个独立参数,第 2 个参数是「范数类型」不是坐标 |
| `np.linalg.norm[3, 4]` | ❌ `TypeError: not subscriptable` —— 函数不能用方括号取下标 |
| `np.linalg.norm([3, 4])` | ✅ `5.0` —— 把**一个列表**交给函数 |

---

## 五、看到报错怎么办

**报错不是失败,是信息。** 从下往上读:

```
NameError: name 'e1' is not defined
└─ 错误类型 ─┘ └────── 具体原因 ──────┘
```

常见三种:

| 报错 | 意思 | 怎么修 |
|---|---|---|
| `NameError: name 'x' is not defined` | 变量没定义过 | 往上翻,把定义那行先跑一遍 |
| `ModuleNotFoundError: No module named 'numpy'` | 库没装 | `pip install -r requirements.txt` |
| `TypeError` / `ValueError` | 参数给错了 | 检查括号类型和参数个数 |
| `UnicodeDecodeError: 'gbk' codec can't decode...` | 文件编码与系统locale不符 | 见下方专条 |

### 专条 · `pip install -r` 报 UnicodeDecodeError

中文版 Windows 的 locale 是 **GBK**,而 pip 读 `requirements.txt` 时用的正是
系统 locale 编码。如果文件里有 **UTF-8 编码的中文**,pip 在**读文件阶段就会崩**,
根本没开始装东西:

```
UnicodeDecodeError: 'gbk' codec can't decode byte 0x80 in position 30
```

> 前面那一大堆 `File "...pip\_internal\..."` 是 pip 的内部调用栈,**不用看**,
> 只看最后一行。

**本仓库的 `requirements.txt` 已改为纯 ASCII,不会再触发。**
万一还遇到,绕过文件直接装即可:

```powershell
pip install numpy matplotlib pandas scikit-learn seaborn networkx sympy scipy statsmodels jupyter ipykernel
```

⚠️ **给助教的提醒:凡是会被程序读取的配置文件**(`requirements.txt`、
`.gitattributes` 等)**一律只写 ASCII**,中文说明放到 `.md` 文档里。
`.py` 文件不受影响——Python 3 默认按 UTF-8 读源码。

**把报错原样发给助教,不用自己硬扛。**
