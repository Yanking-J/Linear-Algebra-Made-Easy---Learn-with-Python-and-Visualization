# LA_01_03《向量加减法》· 练习

> 做法:先自己想 / 写答案,再跑代码验证。答案可以直接写在本文件下方,或在对话里发给助教批改。
> 跑代码前先装环境:在项目根目录 `pip install -r requirements.txt`。
> 配套讲义:`讲义/LA_01_03_向量加减法.html`(用浏览器打开,公式和图都有渲染)。

---

## 课堂已答(存档)

学习过程中当场答过的题,答案已批改,留作复习。

| 题 | 问题 | 你的答案 | 结果 |
|---|---|---|---|
| Q1 | `a=[1,2,3]ᵀ, b=[3,4,5]ᵀ, c=[5,6,7]ᵀ`,求 `a+b+c` | `[9,12,15]ᵀ` | ✅ |
| Q2 | `a=[3,0]ᵀ`,`b`是零向量,平行四边形法则还画得出来吗?`a+b`等于什么? | "不行,画出来是[3,0]的一条向量" | ✅ |
| Q3 | 三个向量 a、b、c 依次首尾相接,换成 c、b、a 顺序相接,终点相同吗? | "一样的" | ✅ |

**要点回顾:** Q2 说明零向量(或共线向量)撑不起平行四边形,加法退化成一条线段;Q3 提前验证了小点⑤的结合律——不管顺序、不管怎么分组,多个向量之和终点不变。

---

## 练习 1 · 加减法计算与验证

已知 `a = [3, 2, 1]ᵀ`,`b = [5, 4, 3]ᵀ`。

1. 手算 `a + b`、`a - b`、`b - a`。
2. 用代码验证:`a - b` 和 `b - a` 是否互为相反数?
3. 用代码验证:`a - b` 是否等于 `a + (-b)`?

```python
import numpy as np

a = np.array([3, 2, 1])
b = np.array([5, 4, 3])

# 在这里写你的代码

```

---

## 练习 2 · 平行四边形法则可视化 a − b

参考讲义图 17(或教材 PDF 第 14 页):向量减法 `a - b` 可以看成 `a + (-b)`,
所以只要把平行四边形法则里的 `b` 换成 `-b`,就能画出 `a - b`。

用 `a = [4, 1]ᵀ`、`b = [1, 3]ᵀ`,仿照 `练习/LA_01_03_代码.py` 里的
`小点2_平行四边形法则()`,画出 `a + (-b)` 的平行四边形。

```python
# 提示:把 b_vec 换成 -b_vec 再画一次平行四边形法则
# 别忘了改 xlim/ylim,因为 -b 会跑到负半轴

```

---

## 练习 3 · 动手画平行六面体(小点③留的坑)

小点③讲平行六面体时说"3D 画图是练习题",现在来填这个坑。

用 `e1=[1,0,0]ᵀ`、`e2=[0,1,0]ᵀ`、`e3=[0,0,1]ᵀ`,画出讲义图 3 那样的
单位立方体,包含三条邻边和主对角线 `e1+e2+e3`。

```python
import numpy as np
import matplotlib.pyplot as plt

fig = plt.figure()
ax = fig.add_subplot(projection='3d')   # 3D 画图的关键:projection='3d'

# 提示: ax.quiver(0, 0, 0, x, y, z, color=...) 画一个从原点出发的 3D 箭头
# 三条邻边 + 一条对角线,一共 4 个 quiver 调用

```

> 卡住了不要紧,这是本节唯一一道 3D 题,查 `matplotlib 3d quiver` 的官方文档或问助教都可以。

---

## 练习 4 · 首尾相连画 RGB 合成(小点⑤的具体版本)

参考讲义图 5 和 PDF 第 12 页图 13:用**三角形法则首尾相连**(不是平行六面体),
依次画出 `e1 → e2 → e3`,验证终点正是 `e1+e2+e3`。

```python
# 提示:这是 2D 就能画的版本(不需要 3D)
# 复用 练习/LA_01_03_代码.py 里 小点4_三角形法则() 的写法:
# 每一段 quiver 的起点,都是上一段 quiver 的终点

```

---

## 我的答案

<!-- 写在这里,或直接发给助教批改 -->
