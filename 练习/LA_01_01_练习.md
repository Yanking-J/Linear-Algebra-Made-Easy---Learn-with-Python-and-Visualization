# LA_01_01《什么是向量》· 练习

> 做法:先自己想 / 写答案,再跑代码验证。答案可以直接写在本文件下方,或在对话里发给助教批改。
> 跑代码前先装环境:在项目根目录 `pip install -r requirements.txt`。

---

## 练习 1 · 预测输出(先别运行,口算)

给定:

```python
import numpy as np
v = np.array([10, 20, 30, 40, 50, 60])
```

写出下面每个表达式的结果:

| 表达式 | 你的答案 |
|---|---|
| `v[0]` | |
| `v[-2]` | |
| `v[1:4]` | |
| `v[:3]` | |
| `v[-2:]` | |
| `v.shape` | |

---

## 练习 2 · 自己写代码

1. 用 `np.array` 创建一个向量 `scores`,表示某同学 5 门课的成绩:`88, 92, 79, 95, 84`。
2. 用**索引**取出第 1 门课的成绩。
3. 用**切片**取出后 3 门课的成绩。
4. 打印这个向量的 `ndim` 和 `shape`。

```python
# 在这里写你的代码

```

---

## 练习 3 · 行 / 列向量 + 转置(连接机器学习)

一朵鸢尾花的四个测量值:`[5.1, 3.5, 1.4, 0.2]`。

1. 用**二维数组、双层方括号**把它写成一个**行向量** `flower`(1 行 4 列)。
2. 它的 `.shape` 是什么?
3. 用 `.T` 把它转成**列向量**,转置后 `.shape` 又是什么?
4. (中文简答)为什么说这朵花是一个"4 维向量"?这和机器学习有什么关系?

```python
# 在这里写你的代码

```

---

## 我的答案

<!-- 写在这里,或直接发给助教批改 -->
