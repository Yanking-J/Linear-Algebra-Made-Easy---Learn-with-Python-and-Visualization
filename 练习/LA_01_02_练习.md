# LA_01_02《坐标系》· 练习

> 做法:先自己想 / 写答案,再跑代码验证。答案可以直接写在本文件下方,或在对话里发给助教批改。
> 跑代码前先装环境:在项目根目录 `pip install -r requirements.txt`。
> 配套讲义:`讲义/LA_01_02_坐标系.html`(用浏览器打开,公式有渲染)。

---

## 课堂已答(存档)

学习过程中当场答过的题,答案已批改,留作复习。

| 题 | 问题 | 你的答案 | 结果 |
|---|---|---|---|
| Q1 | 箭头 A 从 (0,0) 指向 (2,3);箭头 B 从 (1,1) 指向 (3,4)。同一个向量吗? | 相同,大小和方向都一致 | ✅ |
| Q2 | `x1` 3 个数、`x2` 2 个数,`meshgrid` 后 `xx1.shape`? | 以为是 (3,2),实为 (2,3) | ❌ → 已订正 |
| Q3 | `x1` 5 个数、`x2` 9 个数,`xx1.shape`? | (9, 5) | ✅ |
| Q4 | `x = 4·e₁ + (-2)·e₂`,写成列向量并判断方向 | `[4,-2]ᵀ`,右下 | ✅ |
| Q5 | 把 x₃ 轴正方向倒过来,还满足右手定则吗? | 不满足,变成左手系 | ✅ |

**Q2 的订正要点:** `x1` 管水平方向 → 决定**列数**;`x2` 管竖直方向 → 决定**行数**。
而 `shape` 写成 `(行数, 列数)`,所以 `np.meshgrid(x1, x2)` → `shape = (len(x2), len(x1))`,**顺序是反的**。

---

## 练习 1 · 预测输出(先别运行,口算)

```python
import numpy as np

x1 = np.arange(2, 9, 2)
x2 = np.arange(-1, 2, 1)

xx1, xx2 = np.meshgrid(x1, x2)
```

| 表达式 | 你的答案 |
|---|---|
| `x1` | |
| `x2` | |
| `xx1.shape` | |
| `xx2.ndim` | |
| `xx1[0][0]` | |
| `xx2[2][1]` | |

> 提示:`np.arange` 含头不含尾;注意 `step` 不是 1。

---

## 练习 2 · 向量分解与长度

已知 `a = [6, 8]ᵀ`。

1. 把 `a` 写成 `e₁`、`e₂` 的组合(填系数)。
2. **手算** `‖a‖`(写出勾股定理的每一步)。
3. 用 `np.linalg.norm()` 验证。
4. 想让 `a` 变成**单位向量**(长度为 1),该怎么做?写出结果向量并验证它的长度确实是 1。

```python
# 在这里写你的代码

```

> 第 4 问是 1.5 节「单位向量」的预习,自己先试试。

---

## 练习 3 · 换基底(理解 PCA 的种子)

讲义里说过:**坐标不是向量自带的属性,而是拿某组基底去量出来的结果。**

用一组新基底 `b₁ = [2, 0]ᵀ`、`b₂ = [0, 5]ᵀ` 去量向量 `x = [6, 10]ᵀ`。

1. (手算)求 `c₁`、`c₂`,使得 `c₁·b₁ + c₂·b₂ = x`。
2. 同一个箭头,在标准基底 `e₁ e₂` 下坐标是多少?在新基底下又是多少?
3. (中文简答)这两组坐标描述的是**同一个箭头**还是**两个不同的箭头**?为什么?

```python
# 验证代码:可以用 np.linalg.solve
# B = np.column_stack([b1, b2])
# c = np.linalg.solve(B, x)

```

---

## 练习 4 · 手性与行列式

1. 三维标准基底 `e₁ e₂ e₃` 按列拼成矩阵,行列式是多少?
2. 把 `e₂` 和 `e₃` **对调**位置再拼,行列式变成多少?
3. (中文简答)这说明坐标轴的**顺序**对手性有影响吗?

```python
# 提示:np.column_stack([...]) 拼矩阵,np.linalg.det() 求行列式

```

> 这题是第 4 章行列式的预习,算不出来不要紧,重点是观察符号的变化。

---

## 我的答案

<!-- 写在这里,或直接发给助教批改 -->
