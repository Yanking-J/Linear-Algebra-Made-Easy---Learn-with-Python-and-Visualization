# Iris Series "Linear Algebra" - Python dependencies
#
#     pip install -r requirements.txt
#
# IMPORTANT: keep this file ASCII-only. pip decodes it with the system
# locale encoding (GBK on Chinese Windows), so non-ASCII comments raise
# UnicodeDecodeError before installation even starts.
# Chinese notes for each package live in the setup guide (kaike zhunbei).
#
# Python 3.9+ (verified on 3.11). Versions unpinned - latest is fine.

# --- core: used in almost every section ---
numpy
matplotlib

# --- common ---
seaborn
pandas
scikit-learn
networkx

# --- occasional ---
sympy
scipy
statsmodels

# --- required to open and run the .ipynb files ---
jupyter
ipykernel
